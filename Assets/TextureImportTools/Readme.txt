How to use:
1) Navigate to Menu > Texture Import Tools.
2) Select Batch to set import setting for a batch of textures.
3) Select Copy to copy import setting for a single texture or a batch of textures. If Source Texture has Sprite Mode = Multiple, this will also copy a sprite sheet meta (slices, pivots, borders).